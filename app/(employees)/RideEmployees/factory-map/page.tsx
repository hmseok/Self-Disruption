// ═══════════════════════════════════════════════════════════════════
// /RideEmployees/factory-map — 협력공장 지도 (격리 이식본)
// Employee of Ride Inc. 그룹 — FactoryMap 프로젝트 이식
// 메인 컨벤션 호환 (Next.js 16 + React 19 + Tailwind v4)
// ═══════════════════════════════════════════════════════════════════
export const dynamic = 'force-dynamic'

import FactoryMapMain from './FactoryMapMain'

export default function FactoryMapPage() {
  return <FactoryMapMain />
}

'use client'

// Phase G — 구 /finance/codef/page.tsx 재수출 래퍼
// Phase H에서 본문 추출 + violet 틴트 GLASS.L3 적용
import LegacyCodefPage from '../../codef/page'

export default function CodefTab() {
  return <LegacyCodefPage />
}

# operations (사고대차) — CHANGELOG

## 2026-07-10 PR-INTAKE-UX — 접수 상세 개편 (문자 접기 + 상담 사이드 드로어)
- **문자 발송 이력 기본 접힘** (제목에 건수 유지, 클릭 펼침). 카페24 상담내역·긴급출동 메모도 접기 가능.
- **상담 입력 = 우측 사이드 드로어**: 하단 인라인 입력 제거 → 「✍️ 상담 입력」 버튼 → 드로어(최근 상담 3건 맥락 + 큰 입력창 + 카테고리 + Ctrl/Cmd+Enter). 스크롤 위치와 무관하게 입력 가능.

## 2026-07-10 PR-XLSX-RECONCILE — 대차현황 엑셀 재검수 적용 (데이터 작업, 코드 변경 없음)
- 엑셀 605건 vs DB 610건 대조 → 누락 24건 등록(총 634), 반납일 10건 교정(모호 3건 제외), 입금됨→청구완료 8건. 직후 자동매칭 62건 연결. DB에만 있는 15건(이중 등록 의심)은 보존 — 검토 대기.

## 2026-07-08 PR-DEPOSITS-TAB — 사고대차 「입금」 탭 신설 (배차 직원용)
- **탭 4개**: 접수/배차/청구/입금. 입금 탭 = 렌터카통장(매핑 관리 용도에 「렌터카」 포함 계좌)의 입금만 열람 — 원장 페이지는 관리자 영역이라 직원에게 안 엶 (사용자 명시).
- **행 상태 4단계**: 연결됨(대차건 표시) / 후보 있음(입금자명·차량번호 근거 배지) / 미연결 / 사유 처리. 스탯 카드 클릭 = 필터.
- **연결/정리 모달**: 자동 후보 원클릭 연결(완납 시 자동 청구완료 — 기존 훅), 대차건 직접 검색 연결, 「대차 입금 아님」 사유(지입 정산/투자/보험/일반 매출/기타+메모) 저장 → 자동매칭·검수 풀에서 제외, 관리자가 지입·투자·통장 분류에서 이어받아 처리.
- **API**: `GET /api/operations/deposits` 신설, `PATCH /api/transactions/[id]` 에 not_rental 사유 (raw_data $._not_rental — 마이그레이션 불필요), 매처·검수 풀 제외 조건 추가.
- **청구 탭 정리** (사용자 명시 「보기 안 좋아서」): 입금 검수 패널 제거 → 「입금 탭에서 처리 →」 배너로 통합. 통장 페이지 매칭 컬럼에 💰 대차 연결 / 🏷 사유 표시 (관리자 전체 확인).

## 2026-07-08 PR-VERIFY-FIX — 검사기 오탐 2건 수정 (사용자 보고 「시스템 문제」)
- **잔액 사슬 오탐**: 은행이 같은 시각에 일괄 처리한 묶음(세금 납부 등)은 순서를 알 수 없어 건별 비교가 어긋남 → **같은 시각 묶음은 합계로 검사** + 끊긴 뒤 사슬 재동기화.
- **중복 오탐**: 같은 날 정당한 반복 거래(같은 고객·같은 금액 2회)를 중복으로 오인 → 판별자 강화: **시각(초)+거래 후 잔액까지 같아야 쌍둥이** (별개 거래면 잔액이 다름). 확신 불가 행은 검사 제외.
- **계좌별 표시·필터**: 리스트 API에 account_last4 탑재, 통장 탭 계좌 select + 리스트에 끝4자리 표시, 카드 탭 카드별·소지자별 select, 업로드 파일칩에 인식된 계좌 표시 (「계좌 미인식」 경고).

## 2026-07-08 PR-CLEAN-TABS — 통장/카드 = 원장만 (사용자 명시 「확실하지 않은 기능은 혼란」)
- 「대차료 입금현황」 「정산 연결」 탭 완전 삭제 (관련 죽은 코드 포함) — 대차료 업무는 사고대차 청구 탭, 정산은 지입·투자 페이지 (3층 원칙).
- 버튼 재배치: 「🧹 중복 정리」 → 통장 탭 (원장 관리), 「🔗 입금 자동매칭」 → 청구 탭 (업무).
- 통장 엑셀 계좌번호는 파일 상단에서 자동 인식 (입력·파일명은 보조).

## 2026-07-08 PR-ACCOUNT v2 — 카드도 카드별 (사용자 명시)
- account_last4 를 카드에도 사용: 문자 카드/은행 거래는 별칭·원문 *번호에서 끝4자리 자동 추출 저장, 카드 엑셀 업로드도 「이 파일의 카드 끝 4자리」 지정.
- 이후 카드별 필터·카드사 명세 대조의 기반. (V10 컬럼 공용)

## 2026-07-08 PR-ACCOUNT + PR-BANK-RESET — 계좌별 관리 + 통장 새로 정리
- **V10**: `transactions.account_last4` (⏳ 적용 대기) — 계좌별 필터·계좌 단위 잔액 사슬 검증의 기반.
- 통장 엑셀 업로드에 「이 파일의 계좌 끝 4자리」 입력 (비우면 파일명에서 추출). 오픈뱅킹 수집도 계좌 저장. 잔액 맞춰보기에 계좌 입력 — 계좌 단위 검사 (은행 단위 사슬 섞임 해소).
- **통장 전체삭제 = 3종 통합**(엑셀+문자+오픈뱅킹, `bank_all`) — 외주정산·카드 보존, 복구 가능. 확인창 쉬운 말.
- 새로 정리 절차: 은행 전체기간 엑셀(잔액 포함) 준비 → 전체삭제 → 계좌 지정 업로드 → 자동매칭 → 잔액 맞춰보기 ✅.

## 2026-07-07 PR-SOURCE-ONE + PR-RECONCILE — 자료 신뢰 체계 (사용자 명시 「자료가 맞나 의심」)
- **정본 입구 하나**: 오픈뱅킹 연동된 은행(우리 0020/국민 0004)의 입금 문자는 거래로 저장하지 않음 — 문자는 알림 역할, 수신 즉시 오픈뱅킹 동기화를 깨워 정본(codef_bank)으로만 유입. 중복 원천 차단. 미연동 은행은 기존대로 문자 저장.
- **잔액 사슬 자동 검증** (사용자 아이디어 「문자에 잔액 뜨니 자체 누락확인」): 문자·오픈뱅킹의 「잔액 N원」을 balance_after 에 저장 → 통장 탭 「🧮 잔액 맞춰보기」: 기간·은행 고르면 ①잔액 사슬 검사(끊긴 지점 = 누락/중복 위치 특정) ②기간 입출금 합계 ③은행 앱 잔액 수동 대조. 전부 쉬운 말.

## 2026-07-07 PR-BANK-DEDUP v2 — 같은 출처 쌍둥이 + 오픈뱅킹 포함
- 남은 중복의 실체: excel_bank ↔ excel_bank (같은 엑셀 거래 이중 유입 — 예: 빈혜인 20,000 x2). 기존 정리는 문자↔엑셀 짝만 봄.
- `findBankSelfDuplicates`: 통장 계열(excel_bank/codef_bank) 안에서 같은 날+금액+이름+적요 2행 이상 → 1행 보존 (매칭 있는 행 > 오픈뱅킹 원본 > 첫 행). 수동 분류 행 보호.
- 정리 풀에 codef_bank 포함 (문자↔오픈뱅킹 이중 수집도 커버). 「🧹 중복 입금 정리」 버튼이 두 유형 모두 처리.

## 2026-07-07 PR-BANK-DEDUP + 규칙 30 (컬럼 분리)
- **통장 이중 수집 발견**: 어제 올린 통장 엑셀(excel_bank)과 SMS 수집(sms_bank)이 같은 입금을 각각 저장 → 검수 패널 후보 25건 중 24건이 중복 쌍. sms-excel-dedup 도구가 카드용(±3분)이라 못 잡던 것.
- dedup 은행 모드: sms_bank 풀 포함 + 같은 날·같은 금액·입금자명 토큰 겹침 필수. 삭제 방향 = 매칭 붙은 쪽 보존 (기본 SMS 삭제 — 엑셀이 입금자명 등 정보 우위). 조회 기간 90→400일.
- **규칙 30 신설 (CLAUDE.md, 사용자 명령)**: 테이블 컬럼 묶어쓰기 금지 — 1컬럼 1값. 적용: 입금현황(입금액/입금일 분리), 청구 탭(과실/청구율/지급액/지급상태 분리, 상태 배지 단독), 배차 탭(청구유형/견적액 분리).

## 2026-07-07 PR-PARTNER-IMPORT — 외주(지입) 정산 엑셀 업로드
- **운영 사실 (규칙 25)**: 빌려타 차량 = 외주 지입 — 대차료가 우리 통장이 아니라 외주사 정산 내역서(엑셀)로 옴. 2025년 건 대량 「미입금」의 실체 = 통장 매칭 대상이 아니었던 것.
- 청구 탭 「📥 외주 정산 업로드」: 차량별 5컬럼 블록 양식(입금일·보험사·고객차·입금액, 블록 상단=대차차량) 자동 파싱 — 실파일 검증 576행.
- import API: dedupe(날짜+금액+설명) → 이중 키(사고차량+대차차량) 즉시 링크 → `imported_from='excel_partner'` (통장 원장과 분리) → 완납 자동 청구완료. dry-run 확인 후 반영.
- 미링크 건은 검수 패널·매처 풀에 합류 (excel_partner 소스 추가).

> 규칙 22 — 매 PR 한 줄 이상. 이전 이력은 `harness-engineering/handover/` 참조.

## 2026-07-05 PR-PAY-DRAWER — 입금현황 행 클릭 = 사이드 드로어 (사용자 명시 「페이지 이동 대신 사이드 모달」)
- RentalDrawer·QuoteCalc·ConsultationTimeline 을 `app/components/` 로 승격 (operations+finance 공용 — 규칙 17).
- 입금현황 행 클릭 → 페이지 이동 대신 대차건 드로어 (상담·일정·청구정보 즉시 확인·수정). onRequestReturn 옵셔널화 (조회 중심 화면은 반납 버튼 숨김).

## 2026-07-05 PR-PAY-TABLE — 대차료 입금현황 상세 테이블화 (사용자 명시 「한 탭 상세 리스트, 일하기 불편」)
- 압축 5컬럼 grid → NeuDataTable 9컬럼 (배차일·사고차량·대차차량·고객·보험사·청구액·입금액/입금일·상태배지·후보) — 전 컬럼 정렬 (규칙 18).
- 매칭필요 행: 후보 최대 2건 각각 근거 배지(입금자명/차량/이름) + 이름·금액·일자 + 🔗 즉시연결. 행 클릭 = 대차 상세(?from=claims). 검수형 연결(수정 포함)은 청구 탭 확정 모달 안내.

## 2026-07-05 PR-PAY-LINK-MODAL — 입금 연결 확정 모달 (사용자 명시 「대차 내용 보여주고 수정하면 확정」)
- 검수 패널 「연결」 → 깜깜이 즉시 연결 대신 **확정 모달**: 입금 정보 + 대차건(기간·상태) 나란히, 보험사·청구유형·접수번호·예상입금자명·최종 청구액 그 자리에서 수정 → [연결+저장 확정] 한 번에.
- 청구액 비면 입금액 자동 제안 + 「입금액으로」 버튼 — 입금액 ≥ 청구액이면 확정 즉시 자동 청구완료.
- 운영 복구 로그 2차: 보험사 미입력 승격 배포 → cron 이 자동 처리 (paid 46→55건, 3,876만원 / 검수 20건 / LOW 2건).

## 2026-07-05 PR-AUTO-LINK v2 — 보험사 미입력 승격 + 역기입
- LOW 11건 전수 분석: 입금자명 파싱·차량 매칭 모두 정확한데 **대차건 insurance_company 가 빈값**이라 mismatch 오판 → 자동 제외되던 유형.
- 승격: 차량 후보 유일 + 대차건 보험사 미입력 + 입금자명에 보험사 명시 → MEDIUM 자동 + **파싱된 보험사를 대차건에 역기입** (COALESCE — 기존 값 보존).

## 2026-07-05 PR-PAY-COL — 지급액 컬럼 + 청구액 0원 처리
- 청구 탭에 「지급액」 컬럼 (매칭 입금 합계, 녹색 + 완납/부분입금/입금 라벨). 청구액·청구정보의 0원은 「미작성」 처리.
- 데이터 사실: final_claim_amount 588건 전부 0 (엑셀 import 에 청구액 없음) — 채우려면 청구 카드 개별 작성(자동 산출) 또는 요금표 일괄 backfill (다음 작업 후보, 7-02 § 4-3). 완납 자동 전이도 청구액이 있어야 동작.
- 운영 복구 로그: SMS 라벨 교정 137건 → 자동매칭 17건 적용 → paid 32→46건 (2,618→3,250만원), 검수 패널 후보 29건.

## 2026-07-05 PR-SMS-BANK — SMS 통장 입금 매칭 풀 누락 버그 수정 (근본 원인 2)
- **버그**: sms-webhook 이 은행 SMS 도 `imported_from='sms'` 하드코딩 → 매처 풀(sms_bank)에서 제외. rebuild/link-cards 는 BANK 구분하는데 웹훅 본체만 누락 (동형 불일치).
- 수정: issuer `/BANK$/` → 'sms_bank' + 은행 입금 수신 즉시 대차 자동매칭 트리거 (CRON_SECRET 체인, codef 훅과 동형).
- **기존 오염 복구**: 배포 후 관리자 「SMS 거래 재생성」(admin/sms-rebuild-transactions) 1회 실행 → 과거 SMS 통장 입금이 sms_bank 로 재생성 → 자동매칭 1회 → 매칭 모수 복구. codef 과거 백필보다 이게 먼저.

## 2026-07-05 PR-PAY-PAYER — 예상 입금자명 사전 입력 → 거의 전자동 (V9)
- 사용자 명시: 「입금자명도 미리 입력해둘 거니 거의 자동매칭만, 수동제외 굳이 X」 — 기각/사유 시스템 대신 사전 입력 방식 채택.
- **V9**: `fmi_rentals.expected_payer` (⏳ 적용 대기). 드로어 청구정보 섹션에 입력.
- 매처 4번째 축: 입금자명 == expected_payer 정규화 일치(유일) → **HIGH 자동** (사용자 지정값이므로 최상 신뢰). 고객명축은 한글·영문 2~20자로 확대.
- 검수 패널 후보도 3축(입금자명/차량/이름) 배지. V9 미적용 DB graceful.

## 2026-07-05 PR-AUTO-LINK — 확실한 입금 자동 연결 승격 (사용자 명시 「맞는 건 자동연결」)
- 매처 자동 적용 축 2개 추가: ① 입금자명에 **차량번호 전체**(예: 69서0895) + 그 차량 대차건 유일 → HIGH 자동 ② 입금자명 == **고객명** 정규화 일치(한글 2~5자) + 대차건 유일 → MEDIUM 자동.
- 애매한 건(다수 후보·뒤4자리만·보험사 mismatch)만 LOW → 청구 탭 검수 패널에 남음.

## 2026-07-05 PR-PAY-REVIEW — 청구 탭 입금 연결 검수 패널
- **매칭 저조 원인 진단**: 미매칭 입금 102건 중 보험사성 5건뿐 — 과거 입금이 DB에 없음 (codef 연동 이전 + 엑셀 4월까지). 파서 문제 아님.
- **후보 탐지 2축 확장** (사용자 명시 「보험사건·일반 사용자명 다 체크, 엑셀 자유 타이핑 분리분석」): ① 차량번호 뒤4자리 ② 고객명 정규화 일치 — fmi-rental-payments 후보에 `match_by: car|name` 태그.
- **청구 탭 검수 패널**: 「🔗 입금 연결 필요 N건」 접이식 패널 — 대차건 + 후보입금(근거 배지: 차량/이름) + 1클릭 연결 → 완납 자동 청구완료 훅 연동. 20건 초과 시 재무 입금현황 안내.

## 2026-07-05 PR-FLOW-CONSULT v2 + PR-LIST-INFO + PR-PAY-CRON
- **중복 제거 (사용자 명시)**: 접수 탭 = 사고접수 원장 — 진행 배지·대차진행 버튼 제거, 대차 미요청(N) 건의 「→ 대차 전환」만 유지 (Y건·진행 건은 「배차 탭」 표기). 배차 탭 필터 = 📞 상담대기 / 🚐 배차 두 개 (기본 상담대기, 전체 제거).
- **리스트 주요정보**: 배차 탭에 「청구정보」 컬럼(유형·과실·견적액), 청구 탭에 「과실·청구율」 컬럼.
- **실시간 입금 자동화 (PR-PAY-CRON)**: `lib/cron-auth.ts` — X-Cron-Secret 인증을 codef/sync → codef/bank → auto-match-fmi-rental 체인에 적용. codef/sync 는 날짜 미지정 시 최근 3일. Cloud Scheduler 주기 호출로 「입금 → 동기화 → 자동 매칭 → 완납 청구완료」 무인 파이프라인. ⚠ 운영 반영 조건: Cloud Run 에 `CRON_SECRET` env + Scheduler job 생성 (README 명령 참조).

## 2026-07-05 PR-FLOW-CONSULT — 배차 탭 상담·배차완료 재편 + 청구 필터 통합
- 사용자 명시: 「접수 → 상담(대차요청은 자동 유입, 미요청은 수동 대차 진행) → 배차입력 → 배차완료」 / 「청구는 전체 맨앞, 처리대상 제거, 청구전·청구중은 청구대상으로 통합」.
- **배차 탭 = 통합 뷰**: cafe24 대차요청이 자동으로 「상담대기」 유입 (버튼 없이). 필터 = 전체 / 📞 상담(대기+진행) / 🚐 배차완료(예정+출고). scope prop 제거.
- 배지: 상담대기 / 상담중 / 배차예정 / 배차완료.
- **청구 탭 필터** = 💰 전체(맨앞, 기본) / 📤 청구대상(청구전+청구중) / ✅ 청구완료. 스탯 카드 동기화.

## 2026-07-05 PR-PAY-SYNC — 입금 자동 반영 + 완납 자동 청구완료
- 그간 매처는 transactions 링크만 걸고 `fmi_rentals.paid_amount`는 아무도 안 써서 항상 빈값이었음 (지급금액·💰 표시 무의미) — 근본 해소.
- **paid_amount 실시간 파생**: fmi-rentals GET에 매칭 입금합계 LEFT JOIN (저장 안 함 — 언링크에도 항상 정합, 규칙 12). payment_status 파생: 완납/부분입금/입금.
- **완납 자동 청구완료**: 청구중 + 입금합계 ≥ 청구액 → settled 자동 전이. 두 진입점 동형(규칙 14): ① auto-match-fmi-rental 적용 직후(결과에 「완납 청구완료 N건」 보고) ② 수동 1클릭 연결(transactions PATCH). 부분입금은 전이 없이 💰 표시만.

## 2026-07-05 PR-STATUS-LANG — 상태 라벨 업무 언어화
- 사용자 명시: 「배차는 상담완료·배차완료, 청구는 청구전·청구중·청구완료, 청구완료에 금액이나 정산 체크」.
- DB status 불변, 라벨만: pending→상담완료 / returned→청구전 / settled→청구완료.
- 청구완료 행 상태 셀에 재무 자동매칭 입금액 표시 (`💰 N만`, 없으면 「입금 대기」) — 입금 = 정산 체크.
- 동형 적용 (규칙 14): ClaimsTab · RentalListTab · RentalDrawer · rentals/[id] · rentals/page.

## 2026-07-04 PR-QUOTE — 상담 단계 견적 저장 + 청구 전파
- **업무 분석 근거**: 청구액 변수 5개(유형·접수번호·과실·청구율·차종군) 중 4개가 상담 단계 확정 — 저장할 자리가 없어 청구 때 재입력하던 구조 해소. (과실=케이스바이케이스, 청구율=보험사별 관행, 견적=상담 대략+청구 최종 — 사용자 확인)
- **V8 마이그레이션**: `operations_dispatch_orders`에 견적 7컬럼 (⏳ 적용 대기 — DATA-MODEL.md).
- **QuoteCalc 공용 컴포넌트** 신규: 배차하기(상담)·드로어·청구 카드 동일 산출기. calcRentalDays/matchLotteRateIdx 단일 소스화 (ClaimsTab 로컬 복사본 제거).
- **배차하기 화면**: 배차 확정 전에도 청구유형·접수번호·견적 입력/저장 (dispatch_orders PATCH, V8 미적용 시 안내 배너 + 기본 필드만 저장). 「↳ 상담 내용에 추가」로 견적 산식 기록 가능.
- **confirm 전파**: 상담 견적 → fmi_rentals (COALESCE — 기존 값 우선). 접수번호는 상담 저장분 우선.
- **드로어**: 💰 청구 정보·견적 섹션 — fmi_rentals 직접 저장 (마이그레이션 불필요).
- API: dispatch-orders 리스트 SELECT `o.*` 전환 (신규 컬럼 자동 포함), PATCH 견적 필드 + 1054 graceful fallback.

## 2026-07-04 PR-UX-SIMPLE — 접수-배차-청구 3단계 단순화
- **탭 4→3**: 접수 / 배차 / 청구 (라벨 단순화). 「사용가능 차량」 탭 버튼 제거 — 배차하기 화면이 이미 사용가능 차량(waiting-vehicles)에서 선택. `?tab=available` 레거시 링크는 계속 동작.
- **버그 복원**: 청구 탭 행 클릭이 상세페이지로 가면서 청구 카드를 열 방법이 없었음 → 행 클릭 = 청구 카드 복원, 상세는 카드 헤더 「전체 편집 →」.
- **컬럼 다이어트**: 접수(캐피탈사·통보자·대차업체·배정공장·접수자 제거), 배차(플릿·입고공장·보험사 → 드로어), 청구(입고공장·보험접수번호·담당자 제거). 제거 정보는 카드·상세에서 확인, 검색은 유지.
- **청구 카드 접기**: 부가세·지급·영업지원은 기본 접힘 (값 있으면 자동 펼침).

## 2026-07-04 PR-UX-CLAIM — 반납→청구 재입력 제거
- **청구 모달 자동 프리필**: 롯데 차종은 대차 차종 문자열 자동 매칭(`matchLotteRateIdx`), 대여일수는 출고~반납 자동 계산(`calcRentalDays`, rental_days 없을 때) → 청구액 산출까지 자동, 「적용」 클릭만.
- **반납 정보 표시**: 청구 모달 참고 정보에 대여기간(출고~반납·일수) + 반납 메모(notes) 추가 — 재입력 대신 확인.
- **반납 → 청구 직행**: 반납 확정 토스트에 「→ 바로 청구 작성」 버튼 → `operations:switch-tab` 이벤트로 탭 전환 + sessionStorage/`?claim=` 로 해당 건 청구 모달 자동 오픈.
- DB·API 변경 없음. 검증: tsc 0 / lint:harness 새 위반 0.

## 2026-07-04 PR-UX-DRAWER — 배차중 드로어 + 상담 타임라인 + 복귀 수정
- **RentalDrawer** (신규): 배차중 탭 행 클릭 → 페이지 이동 없이 우측 드로어. 상담 기록(즉시 저장), 반납예정·담당자 수정, 반납 버튼, 전체 편집 링크. 기존 `GET/PATCH /api/fmi-rentals/[id]` 재사용, DB 변경 없음.
- **ConsultationTimeline** (신규 공용): `consultation_note` 컬럼 그대로, 새 기록은 `[YYYY-MM-DD HH:mm] 내용` 형태로 위에 append. 타임스탬프 없는 기존 텍스트는 「이전 기록」 블록. 드로어 + 상세페이지 양쪽 적용 (규칙 14 동형). 헬퍼 `appendConsultationEntry()` export.
- **복귀 버그 수정**: 상세페이지 ← 목록이 항상 `?tab=claims` 로 가던 것 → 진입 시 `?from=` 전달받아 출발 탭으로 복귀 (RentalListTab→dispatched, ClaimsTab→claims, 직접 진입 폴백 claims).
- 검증: tsc 수정 파일 에러 0 / lint:harness 새 위반 0 (ui-token 1건 발견 즉시 COLORS 토큰 교체).
